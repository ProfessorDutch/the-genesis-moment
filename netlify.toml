[build]
  # Runs the same build script as package.json; output is generated by Nitro's Netlify preset.
  command = "npm run build"
  # The Netlify preset emits static assets into dist/ and server functions into .netlify/functions-internal/.
  # This overrides any UI setting that still points to dist/client.
  publish = "dist"
